# Frontend Design — [Название продукта]

> Визуальная спецификация UI: layout, токены, компоненты, типографика, темы.
> **Спутник:** [frontend-ux-logic.md](frontend-ux-logic.md) — поведение.
> **v3 этого upgrade:** агентный чат / trace — условный блок со ссылкой на [generation.md](generation.md), не ядро любого UI.

Связанные документы: [vision.md](vision.md), [architecture.md](architecture.md), [api-contracts.md](api-contracts.md).

---

## 0. Статус и принципы

- Соответствие ТЗ / vision: [ссылки].
- Стек UI: [framework, стили, примитивы].
- Embeddable-пакет (если есть): имя; зависимость от framework хоста — [запрещена / какая].
- Reference-мокапы (если есть): `docs/concept/refs/` — **только ориентир**, не pixel-perfect. Источник истины — этот текст.

| Файл | Что показывает |
|------|----------------|
| `refs/...` | … |

---

## 1. Дизайн-язык

- Палитра light / dark: **семантические токены**, не сырые hex и не `bg-zinc-500` в прикладных компонентах.
- Типографика, набор иконок (один).
- Радиусы, spacing, тени — из системы токенов / библиотеки примитивов.

---

## 2. Layout

- Общая схема экрана (области, пропорции, resize).
- Breakpoints (если есть). Desktop-first / иное.

Не копировать чужой трёхпанельный кабинет, если продукт другой.

---

## 3. Области / экраны

Для каждой области: состав, состояния (default, empty, loading, error, active).

### 3.1. [Область A]

- …

### 3.2. [Область B]

- …

---

## 4. Компоненты и примитивы

- Список примитивов библиотеки и кастомных компонентов.
- Запрет самописных дублей существующего примитива.
- Единый рендер markdown (если markdown есть): имя компонента.

---

## 5. Агентный чат и trace (если есть)

Только если в продукте есть лента агента. Решения по событиям — в [generation.md](generation.md); здесь — как это выглядит.

- Жизненный цикл блока: streaming (раскрыт) → completed (сворачивается) → ручной toggle.
- Prominent (диалог) vs compact (tools, plan, subagent).
- Compact / Expanded как режим ленты.
- Summary strip после run (счётчики, копирование, ссылка на observability) — [есть / нет].
- Индикатор стрима на табе чата.
- Индикатор индекса / sync (если есть RAG) — [есть / нет].

| `type` события | Иконка / визуальный вес |
|----------------|-------------------------|
| user | … |
| assistant | … |
| tool_call | … |
| [неизвестный] | дженерик-карточка |

---

## 6. Темы и a11y

- Light + Dark + System — [да / нет].
- Focus visible, клавиатура, иерархия заголовков, контраст через токены.

---

## 7. Structured output в UI

Если финальный ответ содержит таблицы / карточки (не только markdown): как рендерим, где маппинг enum → цвет/бейдж.

---

## 8. Out of scope этой версии

- …
