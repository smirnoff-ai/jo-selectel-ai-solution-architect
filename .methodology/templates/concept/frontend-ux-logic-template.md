# Frontend UX Logic — [Название продукта]

> Поведение интерфейса: side effects, persistence, recovery, error / loading / empty.
> **Спутник** [frontend-design.md](frontend-design.md): тот документ — как выглядит, этот — как ведёт себя.
> **v3 этого upgrade:** для агентного UI обязательны решения про Send/Stop, историю и фоновый стрим.

---

## 1. Глобальные принципы

### 1.1. Optimistic UI

Где применяем / где ждём ответ сервера.

### 1.2. Persistence

| Хранилище | Ключ | Что хранится |
|-----------|------|--------------|
| localStorage | … | тема, последние id, … |
| sessionStorage | … | черновики, expand/collapse |
| in-memory | — | стримы, abort controllers |

### 1.3. Recovery при reload

Что восстанавливается из URL / storage / API. Пошагово.

### 1.4. Гонки и идемпотентность

Что происходит при двойном клике, смене контекста во время запроса.

---

## 2. Навигация и смена контекста

- Клик по [сущности] при активном стриме → [стрим живёт / стопаем / спрашиваем].
- Смена пользователя / документа / проекта → …

---

## 3. Чаты (если есть)

| Вопрос | Решение |
|--------|---------|
| Новый чат до первой отправки | [только клиент / сразу POST] |
| Список чатов фильтруется по | [пара контекста] |
| Переключение таба во время stream | [стрим в фоне / нет] |
| Закрытие / удаление чата | [есть / нет в этой версии] |

---

## 4. Ввод и отправка (если есть агентный или LLM UI)

Обязательно, если есть [generation.md](generation.md).

| Вопрос | Решение |
|--------|---------|
| Send / Stop / повтор | [когда какая кнопка] |
| Черновик textarea | [куда пишем, когда чистим] |
| Режимы `invoke` / `stream` / иное | [селектор; когда disabled] |
| После обрыва SSE | [перечитать историю; пользователь повторяет, если нет финала] |

---

## 5. Trace (если есть)

- Auto-scroll / lock / jump-to-bottom.
- Per-event override expand/collapse — где живёт.
- Compact / Expanded — default и persistence.

---

## 6. Индикаторы системы (если есть RAG / sync)

Что поллим, какие состояния иконки, что видит пользователь пока индекс строится.

---

## 7. Ошибки и empty

- Toast vs inline.
- Поведение после 4xx / 5xx / disconnect.

---

## 8. Host integration (если embeddable)

Callbacks (`onError`, deep links), тема от хоста.

---

## 9. Матрица «действие → эффект»

| Действие пользователя | UI | API | Storage |
|-----------------------|-----|-----|---------|
| … | … | … | … |
