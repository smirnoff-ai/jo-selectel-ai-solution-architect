# frontend

UI диспетчера. Тёмная тема по умолчанию. Логин через rewrite на backend.

Интервьюер: корневой [README.md](../README.md) — `make up`, UI на http://localhost:3000.

Локально на Mac:

```bash
make env
make dev-frontend
```
